{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --project tsconfig.json --files src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}